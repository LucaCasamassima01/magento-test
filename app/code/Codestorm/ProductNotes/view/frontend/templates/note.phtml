<?php if ($block->canShow()): ?>
    <style>
        .product-note {
            border: 1px solid #e5e5e5;
            padding: 16px;
            border-radius: 8px;
            background: #fff;
            max-width: 500px;
        }

        .product-note__title {
            margin-bottom: 12px;
            font-size: 18px;
            font-weight: 600;
        }

        .product-note__field {
            margin-bottom: 12px;
        }

        .product-note__textarea {
            width: 100%;
            min-height: 90px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 6px;
            resize: vertical;
            font-family: inherit;
            font-size: 14px;
        }

        .product-note__actions {
            text-align: right;
        }

        .product-note__button {
            padding: 8px 14px;
            border: none;
            background: #2d6cdf;
            color: #fff;
            border-radius: 6px;
            cursor: pointer;
            transition: background 0.2s ease;
        }

        .product-note__button:hover {
            background: #1f56b8;
        }
    </style>

    <div class="product-note">
        <h3 class="product-note__title">Product Note</h3>

        <form class="product-note__form"
              action="<?= $block->getUrl('productnotes/note/save') ?>"
              method="post">

            <?= $block->getBlockHtml('formkey') ?>

            <input type="hidden"
                   name="product_id"
                   value="<?= $block->getProduct()->getId() ?>" />

            <div class="product-note__field">
                <textarea
                    name="content"
                    maxlength="255"
                    class="product-note__textarea"
                    placeholder="Write your note..."></textarea>
            </div>

            <div class="product-note__actions">
                <button type="submit" class="product-note__button">
                    Save
                </button>
                <button type="button"
                        class="product-note__button"
                        onclick="window.location.href='<?= $block->getUrl('productnotes/note/index') ?>'">
                    Go to notes
                </button>
            </div>

        </form>
    </div>
<?php endif; ?>